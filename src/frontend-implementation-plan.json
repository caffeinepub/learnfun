{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Add explicit French and German UI translation dictionaries",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Replace the French (fr) fallback with a complete explicit French dictionary for all existing common UI keys.",
      "acceptanceCriteria": [
        "`frontend/src/lib/translations.ts` contains a `fr` entry with explicit French values for every key in `commonTranslations` (no spreading of `commonTranslations` for `fr`).",
        "All existing keys in `commonTranslations` are present in `translations.fr` and translated to French.",
        "Existing emojis/symbols (e.g., 🎉, ➡️, ⚡) remain present in the French strings where they exist in `commonTranslations`.",
        "No non-French languages are modified except as necessary to add the explicit `fr` dictionary."
      ],
      "file_operations": [
        {
          "path": "frontend/src/lib/translations.ts",
          "operation": "modify",
          "description": "Replace `fr: commonTranslations` with an explicit `fr` dictionary containing French translations for every key currently in `commonTranslations`, preserving existing emojis/icons and keeping the translation key set identical (no added/removed keys)."
        }
      ]
    },
    {
      "id": "REQ-2",
      "summary": "Replace the German (de) fallback with a complete explicit German dictionary for all existing common UI keys.",
      "acceptanceCriteria": [
        "`frontend/src/lib/translations.ts` contains a `de` entry with explicit German values for every key in `commonTranslations` (no spreading of `commonTranslations` for `de`).",
        "All existing keys in `commonTranslations` are present in `translations.de` and translated to German.",
        "Existing emojis/symbols (e.g., 🎉, ➡️, ⚡) remain present in the German strings where they exist in `commonTranslations`.",
        "No non-German languages are modified except as necessary to add the explicit `de` dictionary."
      ],
      "file_operations": [
        {
          "path": "frontend/src/lib/translations.ts",
          "operation": "modify",
          "description": "Replace `de: commonTranslations` with an explicit `de` dictionary containing German translations for every key currently in `commonTranslations`, preserving existing emojis/icons and keeping the translation key set identical (no added/removed keys)."
        }
      ]
    }
  ]
}